# requirements_local.txt
# macOS (Apple Silicon + Intel) and Linux — no Docker, no CUDA.
# PyTorch from PyPI includes MPS support for Apple Silicon automatically.
#
# Install: pip install -r requirements_local.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
python-multipart
jinja2

# ── Vector database (local file mode — no Docker) ────────────────────────────
qdrant-client

# ── Embeddings — torch from PyPI includes Apple MPS support ──────────────────
torch>=2.0.0
sentence-transformers

# ── PDF / document processing ─────────────────────────────────────────────────
PyPDF2
pdfplumber
python-docx
openpyxl
python-pptx
beautifulsoup4
lxml
pymupdf

# ── Cryptography (for encrypted PDFs) ────────────────────────────────────────
pycryptodome
cryptography

# ── LLM (talks to local Ollama) ───────────────────────────────────────────────
ollama

# ── CLI  ──────────────────────────────────────────────────────────────────────
questionary
rich

# ── Data / analytics ─────────────────────────────────────────────────────────
pandas
numpy
rapidfuzz
plotly
kaleido
matplotlib
seaborn
scikit-learn
umap-learn

# ── Database (optional: only used if PostgreSQL is enabled) ──────────────────
sqlalchemy
psycopg2-binary
